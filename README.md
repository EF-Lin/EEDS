# Exploring Earth Data Science Scripts
Provide some scripts that might help with processing data.

## split csv
split csv based on certain key, files would be saved under `\splits` folder.

### Usage
```bash
python split.py "input file" "[optional] output path"
```
